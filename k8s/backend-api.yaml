apiVersion: apps/v1
kind: Deployment
metadata:
  name: alz-platform-api
  namespace: alz-foundation-prod
  labels:
    app: alz-api
spec:
  replicas: 3
  selector:
    matchLabels:
      app: alz-api
  template:
    metadata:
      labels:
        app: alz-api
    spec:
      containers:
      - name: gateway
        image: acralz.azurecr.io/devopstrio/alz-platform-api:v1.0.0
        ports:
        - containerPort: 8000
        env:
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: alz-secrets
              key: db-url
        resources:
          requests:
            cpu: "250m"
            memory: "512Mi"
          limits:
            cpu: "1000m"
            memory: "1Gi"
---
apiVersion: v1
kind: Service
metadata:
  name: alz-api-svc
  namespace: alz-foundation-prod
spec:
  selector:
    app: alz-api
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8000
  type: LoadBalancer
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: governance-engine
  namespace: alz-foundation-prod
spec:
  replicas: 2
  selector:
    matchLabels:
      component: governance-engine
  template:
    metadata:
      labels:
        component: governance-engine
    spec:
      containers:
      - name: worker
        image: acralz.azurecr.io/devopstrio/governance-engine:latest
        resources:
          requests:
            cpu: "500m"
            memory: "1Gi"
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: alz-ingress
  namespace: alz-foundation-prod
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
  - host: foundation.devopstrio.co.uk
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: alz-api-svc
            port:
              number: 80
